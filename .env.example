# App
NEXT_PUBLIC_APP_NAME="Automated Small Claims Nuke"
AUTH_SECRET="replace-me-with-a-long-random-string"
APP_BASE_URL="http://localhost:3000"

# Steam
STEAM_WEB_API_KEY="your-steam-web-api-key"

# Database (self-hosted Postgres on Coolify)
DATABASE_URL="postgres://postgres:postgres@localhost:5432/steam_redeem"
DATABASE_SSL="false"

# Gemini
GEMINI_API_KEY=""
GEMINI_PROVIDER="google_ai"
GEMINI_MODEL="gemini-3-flash"
# Use `gemini-3.1-pro` for higher quality reasoning.

# Vertex AI options
GEMINI_VERTEX_PROJECT=""
GEMINI_VERTEX_LOCATION="global"
# For `vertex_express`, this key is optional if GEMINI_API_KEY is set.
GEMINI_VERTEX_API_KEY=""

# For `vertex` mode, use ADC credentials (service account) on the host.
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"
